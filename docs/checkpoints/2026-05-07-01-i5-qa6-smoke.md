# Checkpoint: I5 QA-6 真实 provider smoke

- **Date**: 2026-05-07
- **Iteration**: I5
- **Task**: QA-6 — 真实 provider smoke（claude + plain + sticky）

## 测试环境

- **Provider**: claude (Claude Code CLI 2.1.131)
- **OS**: macOS Darwin 24.6.0
- **Workspace**: 临时 `/tmp/ralph-qa6-smoke` / `/tmp/ralph-qa6-smoke-sticky`
- **ralph**: 0.1.1-dev (e19110b)

## Test 1: Plain 模式 — `ralph run --provider claude`

### 命令
```
cd /tmp/ralph-qa6-smoke && bash .ralph/bin/ralph run --provider claude --round-timeout 90
```

### 结果
- Exit code: **0** (done)
- Duration: **17s** (1 round)

### 输出（20 行 ≤ 50 行限制）
```
[00:19:26] ralph 0.1.1-dev | run 20260506-161926-2b3e0f6 | I1 | tasks 0/1 done | provider claude
[00:19:26] round 1/∞ → DEV-1: 创建 hello.txt 文件内容为 "hello from ralph"
[00:19:43] round 1/∞ ✓ done | tasks 1/1 | round 16s | run 17s
────────────────────────────────────────────────
Ralph Run Complete
────────────────────────────────────────────────
Run ID:        20260506-161926-2b3e0f6
Iteration:     I1
Exit Reason:   done
Rounds:        1
Tasks:         1 / 1
Started:       2026-05-07 00:19:26 +0800
Duration:      17s
All tasks completed.
────────────────────────────────────────────────
```

### 结构验证

| 检查项 | 结果 |
|--------|------|
| Banner 含 version/run_id/iteration/tasks/provider | ✅ |
| Round start marker: `round N/∞ → TASK` | ✅ |
| Round end marker: `round N/∞ ✓ done \| tasks M/N \| round Xs \| run Ys` | ✅ |
| Summary block: Run ID / Iteration / Exit Reason / Rounds / Tasks / Started / Duration | ✅ (7 字段) |
| exit-message.txt 全字段 | ✅ (9 个关键字段) |
| 无 ANSI 转义码 | ✅ |
| 行数 ≤ 50 | ✅ (20 行) |

### 目录结构
```
.ralph/runs/20260506-161926-2b3e0f6/
  context.json
  exit-message.txt
  result.json
  rounds/round-001/
    meta.json
    provider.stdout.log
    session.claude.jsonl
    session.history.log
  TASKS.md
```

### JSON 字段验证

| 文件 | 关键字段 | 值 |
|------|----------|-----|
| result.json | `rounds` | 1 (非 iterations) |
| result.json | `exit_reason` | "done" |
| result.json | `iteration_name` | "I1" |
| meta.json | `round` | 1 (非 iteration) |
| meta.json | `changed_files_round` | ["hello.txt"] (非 changed_files_iter) |
| meta.json | `stall_count` | 0 (非 stagnation_count) |
| meta.json | `capture_status` | "ok" |
| context.json | `max_round` | 0 (非 max_iter) |
| context.json | `stall_limit` | 5 |

### Session 捕获
- `session.claude.jsonl`: 19986 bytes ✅
- `session.history.log`: 存在 ✅
- `provider.stdout.log`: 有效 JSONL 流（含 init/conversation/result 事件）✅

### 功能验证
- TASKS.md 任务正确勾选 `[x]` ✅
- hello.txt 内容 "hello from ralph" ✅
- Git commit 由 claude agent 自动创建 ✅

---

## Test 2: Sticky 模式 — `ralph run -v --provider claude`

### 命令
```
cd /tmp/ralph-qa6-smoke-sticky && script -q /tmp/ralph-qa6-sticky.log bash .ralph/bin/ralph run --provider claude -v --round-timeout 90
```

### 结果
- Exit code: **0** (done)
- Duration: **28s** (1 round)

### Sticky 渲染验证（通过 ANSI 转义码分析）

| 检查项 | 结果 |
|--------|------|
| 三段式布局（顶栏 + 事件区 + 底栏） | ✅ |
| 顶栏字段: version / tasks / oneshots / provider / elapsed | ✅ |
| 分隔线 `───` | ✅ |
| 事件区 6 行（含 ⚙ 工具事件） | ✅ |
| 底栏字段: ● / round N/∞ / stall N/5 / spinner / elapsed / task名 | ✅ |
| 健康灯: 绿色 ● (ANSI 32) | ✅ |
| Spinner 动画: ⠙⠹⠸⠼⠴⠦ | ✅ |
| cursor_up 重绘 (^[[10A]) | ✅ |
| 终端还原: cursor show (^[[?25h) + reset (^[[0m]) | ✅ |
| 退出后 "Ralph Run Complete" summary | ✅ |
| 事件含中文截断显示正常 | ✅ |

### 目录结构
与 plain 模式一致（rounds/round-001/ 目录，全部 JSON 字段正确）。

### JSON 字段
- result.json: `rounds=1, exit_reason=done`
- meta.json: `round=1, stall_count=0, capture_status=ok`
- session.claude.jsonl: 19986 bytes

---

## 项目检查

| 检查 | 结果 |
|------|------|
| `bash scripts/check.sh` | ✅ passed |
| `bash -n scripts/integration-test.sh` | ✅ OK |

---

## 未验证项（需人工在真实终端完成）

1. **Sticky 视觉保真度**: 与 `ralph-sticky-poc.sh` 的视觉对比（颜色渲染、对齐、CJK 字宽）需在真实终端中人工确认
2. **健康灯黄/红态转换**: 需要超过 60s 和 300s 的长运行任务触发
3. **Ctrl+C 退出后 sticky 块保留**: 需交互式终端人工测试
4. **异常退出 stty 还原**: kill -9 / 终端关闭后的 stty 恢复（已知限制：kill -9 不触发 trap）
5. **tmux/screen 下真实 provider 长时间运行**: QA-5 已验证结构，但真实 provider 长时间视觉效果未测
