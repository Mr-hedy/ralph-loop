# 目标与约束

- 保留 T6（v0.1 闭环验证 + 使用指南）prep 完成的可用状态，作为 T6.0–T6.7 实施起点的回滚锚点。
- 约束：纯文档 + 部署单元配置改动（无代码改动）；T6 入场前 adversarial review 已暴露 T2 漏检的两个 P0 缺陷（stagnation cumulative + effort 漏接入），独立子任务 T6.1 / T6.2 修复，PM-0003 沉淀流程预防机制。

# 范围

- T6 阶段规划全集：roadmap 重排序（T2 → T6 提前 → T3/T4/T5 后置）、requirements 轮次 4 + REQ-017 + SC-017-1、task.md 替换 T2 块为 T6.0–T6.7 八个子任务、AGENTS.md（CLAUDE.md）项目身份重申"`.ralph/` 整个目录是产物"。
- 部署单元配置：`.ralph/.gitignore` 删除（部署单元自身不带运行期忽略规则）。
- Postmortem：PM-0003 新增（任务收口 REQ traceability + 现实条件外推预防机制）。
- 不含：任何代码改动；T6.0–T6.7 任一子任务的实施。

# 核心变更

- `docs/roadmap.md`：阶段表 T6 提前到 T2 之后 + "阶段重排序决策（2026-04-28）"理由块；新增"T6 范围"段（覆盖需求 / 交付 / 退出条件 / 非范围）；删除已过期的"T1 范围"段；Current State 同步至 T2 完成 + T6 当前阶段。
- `docs/requirements/ralph-loop/requirements.md`：澄清记录新增"轮次 4（交付单元 / 部署形态，2026-04-28）"narrative；新增 REQ-017（交付单元 = `.ralph/` 整个目录）；新增 SC-017-1（cp -r 部署链路验收）；REQ-008 amend 引用 REQ-017；§非目标 line 23 边界澄清"用户自行创建"语义；validation table 加 REQ-017 行。
- `AGENTS.md`（= `CLAUDE.md` symlink）：Project Identity 增"项目最终产物 = `.ralph/` 整个目录"；Top Rules 把 PROMPT.md/TASKS.md 从"不入仓"清单移到"入仓样板"；明确 runs/lock/status.json/.env 由外层 .gitignore 处理；root task.md vs `.ralph/TASKS.md` 双源职责区分明确化。
- `task.md`：T2 块下线（commit `0cd99ab` 已落仓 T2，task.md 不保留历史）；T6 段拆 8 子任务，全部决策落实文档（T6.1 stagnation 方案 B 已选 + Rejected A/C 理由；T6.4 stagnation 用例改为等待型任务；T6.4 timeout 用例改为 sleep 30；TASKS 模板独立放 `.ralph/TASKS.md`）；T6.1 / T6.2 显式引用 PM-0003。
- `handoff.md`：完整刷新到 T6 prep 完成状态；包含 8 子任务依赖顺序、stable decisions、PM-0003 关联、下一步建议。
- `.ralph/.gitignore`：**删除**（部署单元自身不带运行期忽略规则；REQ-017 明确由使用者外层 .gitignore 处理 runs/lock/status.json/.env）。
- `docs/postmortems/pm-task-closure-req-traceability.md`：**新增** PM-0003（任务收口 adversarial review 缺乏 REQ traceability + 现实条件外推 → T2 漏检两个 P0）。
- `docs/postmortems/README.md`：条目表加 PM-0003。

# 影响文件或模块

- `docs/roadmap.md`
- `docs/requirements/ralph-loop/requirements.md`
- `AGENTS.md`（= `CLAUDE.md`）
- `task.md`
- `handoff.md`
- `.ralph/.gitignore`（删除）
- `docs/postmortems/pm-task-closure-req-traceability.md`（新增）
- `docs/postmortems/README.md`

# 稳定决策

- **REQ-017 交付单元 = `.ralph/` 整个目录**：含 `bin/` + `lib/` + 参考样板 `PROMPT.md` + `TASKS.md`；部署 = `cp -r .ralph/ <workspace>/.ralph/`；运行期产物（runs/lock/status.json/.env）由使用者外层 .gitignore 管理；本仓库不自跑 ralph 故 `.ralph/` 不含 runtime artifacts。
- **REQ-008 + REQ-017 边界**：REQ-008 改为引用 REQ-017 描述部署单元构成。
- **§非目标 line 23 边界澄清**：约束 ralph 工具运行时**不**写 PROMPT/TASKS/.env、**不**读取它们做内核控制流；样板入仓由人工维护、由 `cp -r` 部署，不是 init 路径。
- **阶段重排序**（roadmap §阶段重排序决策）：T2 → T6（提前）→ T3 → T4 → T5 → T7。
- **T6 子任务依赖顺序**：T6.0 → T6.1 → T6.2 → T6.3 → T6.4 → T6.5 → T6.6；T6.7 patch 池贯穿。
- **T6.1 stagnation 比对方案 = B（in-memory 哈希）**：`git ls-files -s` + `git status -z` 拼起来过 `sha256sum`；不写 refs / 不创建 git 对象。
- **T6.4 stagnation 真触发用例 = 等待型任务**（rejected 歧义任务）；**T6.4 timeout 用例 = `Bash sleep 30` + `--timeout=10`**（rejected 5 万字文档）。
- **PM-0003 预防机制**：任务收口 adversarial review 必须含 REQ traceability rerun + mock 假设外推；checkpoint 验证段必须按"SC-NNN-N → 测试用例名 → PASS"对账；mock fixture 注释必须列简化假设。

# 验证结果

- 命令：`bash scripts/check.sh`
- 结果：通过
- 诊断：`ralph-loop check passed`；本轮纯文档变更无代码改动，未跑 `bash scripts/integration-test.sh`（不必要）；`grep "REQ-23\b"` 全清（之前对 REQ-23 的错引已修复为 `requirements §非目标 line 23` 或 REQ-017）；`grep "REQ-017"` 在 task.md / roadmap / requirements / handoff 四方一致；`git diff --check` 无空白错误。

# Postmortem Sweep

- 结果：**已新增 PM-0003**
- 关联：[docs/postmortems/pm-task-closure-req-traceability.md](../postmortems/pm-task-closure-req-traceability.md)；既有 PM-0001（macOS shell 兼容，本轮全程遵循）、PM-0002（跨任务决策沉淀，本轮新增 REQ-017 / 轮次 4 narrative / PM-0003 引用均按 PM-0002 规则沉淀）。
- 说明：本轮 T6 入场 adversarial review 暴露 T2 漏检两个 P0 缺陷（stagnation cumulative bug + effort flag 漏接入），与 T2 done checkpoint 声明"无新失败模式"矛盾。根因不是实现 bug 而是任务收口 adversarial review 流程缺 REQ traceability rerun + mock 假设现实条件外推，可复用失败模式，沉淀为 PM-0003。两个 P0 的修复独立为 T6.1 / T6.2 子任务（不挂 T6.7 patch 池，保留可见性），完成后回查 PM-0003 验证预防机制有效。

# 未验证范围与风险

- T6.0–T6.7 任一子任务的实施成果**未验证**（这是下一轮的工作）。
- T6.1 stagnation 方案 B（in-memory 哈希）的实际性能 / macOS BSD `git ls-files` 行为差异**未验证**（T6.1 实施 + 集成测试覆盖）。
- T6.2 effort 翻译数值（low/medium/high → thinking-budget=2000/8000/16000）**未对照当前 Claude CLI 合法范围**（T6.2 实施时查 `claude --help` 确认）。
- REQ-017 部署链路（`cp -r .ralph/ <workspace>/.ralph/` 跑通）**未实证**（T6.3 真实多轮 smoke 验证 SC-017-1）。
- PM-0003 预防机制**未在新任务收口实战**（首次实战是 T6.6 v0.1 closure 阶段）。
- `.agents/skills/checkpoint/SKILL.md` 是否纳入 PM-0003 预防检查**待用户裁决**（不在 T6 范围）。

# 下一步

1. 进入下一会话起手 T6.0：起草 `.ralph/PROMPT.md`（≤120 行，参考 `/Users/hedy/Develop/code/trantor-project/trantor-skills/.trantor/build/PROMPT.md` 提炼但去 trantor 化）+ `.ralph/TASKS.md`（≤20 行 hello-world 起手示例）+ `docs/README.md` 索引同步。
2. T6.0 完成后按依赖顺序 T6.1（stagnation 方案 B 实施）→ T6.2（effort 接入）→ T6.3（多轮 smoke）→ T6.4（边界复验）→ T6.5（usage doc）→ T6.6（v0.1 closure）。
3. T6.1 / T6.2 完成时回查 PM-0003，验证修复让真实长链路 stagnation 触发 + effort 接入通过；T6 阶段所有任务收口 checkpoint 必须按 PM-0003 预防机制做 REQ traceability rerun。
