# 目标与约束

- T2（Claude adapter）准备阶段全套产物落仓：任务拆解、横切设计、adversarial review 修复、跨任务决策沉淀、新 postmortem。作为 T2 实施开工前的回滚锚点。
- 约束：本 checkpoint 仅含文档与协作规则改动，**不含任何代码改动**；T2.0–T2.6 代码尚未实现。

# 范围

- 任务事实源：`task.md`（T1 已勾内容已清，新一轮 7 个 T2 任务全为未勾）。
- 架构文档：`docs/architecture/overview.md`（伪代码同步）、`docs/architecture/integrations.md`（新增 Session 文件命名约定段）。
- 协作规则：`.spec/rules/testing.md`（新增测试隔离 + 测试基础设施单一来源两段）。
- Postmortem：`docs/postmortems/pm-cross-task-decision-sedimentation.md`（PM-0002 新建）+ README 索引。
- Roadmap：`docs/roadmap.md`（新增 Deferred 段）。
- Handoff：`handoff.md`（同步刷新到 T2.0 起点）。

# 核心变更

- T2 任务拆解 7 任务（T2.0–T2.6）：依赖校验框架 / Claude adapter 骨架 + mock-claude 单一来源 / Session 采集 + cwd_hash + setup_claude_workspace helper / 错误诊断矩阵 / 派生视图 / 集成测试扩展 / 真实 smoke + `--version` + `--help` 补全。
- 跨任务横切决策沉淀到稳定文档（不放 task.md）：
  - Session 文件命名约定 `session.<provider>.*` → `integrations.md`
  - 测试隔离规则（HOME/PATH/配置/网络）+ 测试基础设施单一来源 → `.spec/rules/testing.md`
- PM-0002 沉淀 meta-pattern：跨任务决策只在对话/任务文档中提及未沉淀稳定文档，下一轮容易偏离；含 4 类典型表现 + 预防检查。
- Roadmap Deferred 段：`ralph doctor` + provider 版本检查后置 v0.2/T7（T2.0 框架预留 `min_version` 参数位）。
- task.md 顶部规则段加强制：跨任务稳定决策必须沉淀到 `docs/architecture/*` 或 `.spec/rules/*`，task.md 只引用锚点。

# 影响文件或模块

- 修改：`task.md`、`docs/architecture/overview.md`、`docs/architecture/integrations.md`、`docs/postmortems/README.md`、`docs/roadmap.md`、`.spec/rules/testing.md`、`handoff.md`。
- 新增：`docs/postmortems/pm-cross-task-decision-sedimentation.md`、`docs/checkpoints/2026-04-27-02-t2-prep.md`。

# 稳定决策

- 依赖校验框架三件套（`ralph_require_cmd` + `ralph_report_missing_deps` + `provider_check_deps`），`min_version` 预留位本期不实现。
- Session 文件命名约定 `session.<provider>.*`（Claude stdout = `session.claude.stdout.json`）。
- meta.json 复杂字段写入用 jq `update_meta_jq`，禁用 sed 拼装对象。
- mock-claude 单一来源 `tests/fixtures/mock-claude`：T2.1 建骨架，T2.5 扩场景。
- 测试基础设施单一来源 `setup_claude_workspace`：T2.2 建立，T2.5 扩展。
- HOME / PATH 强制隔离（`.spec/rules/testing.md`）。
- cwd_hash 严格步骤顺序（先 realpath → 再字符替换）。
- 当前开发阶段 `.ralph/.env` 默认 `RALPH_PROVIDER=fake`；T2.0/T2.1 用 fake，T2.2-T2.5 用 mock-claude，T2.6 真实 claude。
- 未实现命令（status/watch）行为统一：占位 help + exit 0；T2.6 新增 `ralph --version` / `-v`。
- 派生视图 schema 严格遵循 `docs/architecture/overview.md` 既有定义，不自定义。

# 验证结果

- 命令：`bash scripts/check.sh && bash scripts/integration-test.sh && git diff --check`
- 结果：通过
- 诊断：`check.sh` → `ralph-loop check passed`；`integration-test.sh` → `PASS=14 FAIL=0`（T1 无回归）；`git diff --check` 无空白错误。

# Postmortem Sweep

- 结果：已新增
- 关联：`docs/postmortems/pm-cross-task-decision-sedimentation.md`（PM-0002）；继承引用 `docs/postmortems/pm-shell-macos-compat.md`（PM-0001，T2 shell 代码硬约束仍生效）。
- 说明：本轮 adversarial review 暴露 4 类同根因问题（命名约定 / 测试基础设施位置 / 步骤顺序 / 测试隔离），都属于"跨任务稳定决策未沉淀到稳定文档"。已沉淀到 PM-0002 + 触发对应稳定文档更新（integrations.md 命名约定段、testing.md 隔离与单一来源段）；并在 task.md 顶部规则加强制条款。另外发现一次"adversarial review 时未读全 docs 误报缺失"（chat.log/tools.log schema 实际已在 overview.md 定义），同样写进 PM-0002 作为 review 自身的提醒。

# 未验证范围与风险

- T2.0–T2.6 任何代码尚未实现，本 checkpoint 不含代码验证。
- `tests/fixtures/mock-claude` 行为、HOME/PATH 隔离机制、依赖框架聚合报错、cwd_hash 严格顺序的真实有效性 → T2.0/T2.1/T2.2 实施时验证。
- 真实 Claude CLI 行为偏差（jsonl schema、cwd_hash 实际算法、stdout JSON 字段）→ T2.6 smoke 暴露后补丁。

# 下一步

- 在新 session 中按 `task.md` 顺序开工 T2.0「依赖校验框架」：实现 `ralph_require_cmd` / `ralph_report_missing_deps` / fake `provider_check_deps`，run.sh 接入 + 删除原 `command -v` 校验段，overview.md 伪代码同步，integration-test.sh 断言改 + 新增 ≥3 用例。完成后 macOS 实测 `bash scripts/check.sh && bash scripts/integration-test.sh` 全绿才能勾选 T2.0。
