# 目标与约束

- T6.3 真实多轮 smoke：用真实 Claude CLI 在临时 workspace 跑通 3 条独立任务，验证 T6.0 模板有效 + T6.1 stagnation 修正 + T6.2 effort 接入在真实链路下无回归。
- 约束：独立 workspace `/tmp/ralph-t6-multi-smoke/`，不污染本仓库；证据保全后清理 /tmp。

# 范围

- smoke workspace 部署：`cp -r .ralph/ /tmp/ralph-t6-multi-smoke/.ralph/`（REQ-017 SC-017-1 验证）。
- 3 条任务：(a) hello.txt；(b) README.md；(c) notes/architecture.md。
- `.env`：`RALPH_PROVIDER=claude`、`RALPH_EFFORT=low`（验证 T6.2 effort 接入）、`RALPH_MAX_ITER=10`。
- PROMPT.md：直接用 T6.0 样板，不改。

# 核心证据

## run result.json

```json
{
  "run_id": "20260428-080709-a17ef14",
  "exit_reason": "done",
  "iterations": 4,
  "tasks_total": 3,
  "tasks_checked_start": 0,
  "tasks_checked_end": 3,
  "started_at": "2026-04-28T08:07:09Z",
  "finished_at": "2026-04-28T08:08:18Z",
  "duration_sec": 70,
  "last_error": null
}
```

## iter-001 meta.json（关键字段）

```json
{
  "iteration": 1, "exit_code": 0, "capture_status": "ok",
  "changed_files_iter": ["hello.txt"],
  "changed_files_total": ["hello.txt"],
  "tasks_before": {"total": 3, "checked": 0},
  "tasks_after": {"total": 3, "checked": 1},
  "stagnation_count": 0,
  "session_id": "113da227-3b7d-4286-99b2-21027211e6d4"
}
```

## iter-002 meta.json（关键字段）

```json
{
  "iteration": 2, "exit_code": 0, "capture_status": "ok",
  "changed_files_iter": ["README.md"],
  "changed_files_total": ["hello.txt", "README.md"],
  "tasks_before": {"total": 3, "checked": 1},
  "tasks_after": {"total": 3, "checked": 2},
  "stagnation_count": 0
}
```

## iter-003 meta.json（关键字段）

```json
{
  "iteration": 3, "exit_code": 0, "capture_status": "ok",
  "changed_files_iter": ["notes/architecture.md"],
  "changed_files_total": ["hello.txt", "notes/architecture.md", "README.md"],
  "tasks_before": {"total": 3, "checked": 2},
  "tasks_after": {"total": 3, "checked": 3},
  "stagnation_count": 0
}
```

## 目标文件内容

- `hello.txt`：`hello ralph`
- `README.md`：`# Smoke Test Project\n- 2026-04-28: initial setup`
- `notes/architecture.md`：`# Architecture\n- 组件：ralph loop runner\n- 部署：cp -r .ralph/ <workspace>/.ralph/`

# 环境信息

- Claude CLI 版本：`2.1.121 (Claude Code)`
- run_id：`20260428-080709-a17ef14`
- workspace：`/tmp/ralph-t6-multi-smoke/`（已清理）
- cwd_hash：`-private-tmp-ralph-t6-multi-smoke`

# 断言结果

| 断言 | 结果 |
|------|------|
| exit_reason=done | ✓ |
| iterations ≥ 3 | ✓（4 轮） |
| last_error=null | ✓ |
| 每轮 capture_status=ok | ✓ |
| 每轮 stagnation_count=0 | ✓ |
| changed_files_iter 仅含本轮变更（T6.1 修正验证） | ✓ |
| changed_files_total 累计递增 | ✓ |
| 3 个目标文件存在且内容合理 | ✓ |
| RALPH_EFFORT=low 无异常（T6.2 接入验证） | ✓ |
| REQ-017 SC-017-1：cp -r 一次性部署跑通 | ✓ |

# 未验证范围

- T6.4 边界场景（stagnation / timeout / max_iter 在真实 Claude 下触发）→ 下一步。
- T6.0 PROMPT.md 样板对 Claude 跟住协议的稳定性（单次 smoke 无法证明，需多次复现）。

# 下一步

进入 T6.4：stagnation / max_iter / timeout 三种边界场景在真实 Claude 下各触发一次。
