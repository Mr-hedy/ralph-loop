# 目标与约束

- 保留 Ralph v0.1 T0（Bootstrap）完成、T1（Run 骨架 + Adapter 契约 + Fake 实现）启动前的稳定锚点。
- 需求、架构、安全边界、provider 集成细节和 roadmap 已按 `.spec` 规范固化，T1 可在此之上开工；未来任何回退"需求/架构再澄清"的决策都应以本 checkpoint 为起点对比。

# 范围

- 协作规范与文档结构：`.spec/`、`.agents/skills/`、`.claude/skills/`、`AGENTS.md`、`CLAUDE.md`（→ AGENTS.md 符号链接）、`README.md`、`docs/README.md`。
- 项目级与模块级需求：`docs/requirements.md`、`docs/requirements/ralph-loop/requirements.md`。
- 架构稳定契约：`docs/architecture/overview.md`、`docs/architecture/integrations.md`、`docs/architecture/security.md`。
- 专项规划与任务：`docs/roadmap.md`、`task.md`、`handoff.md`。
- 工具骨架与结构性检查：`.ralph/bin/ralph`（仅 `help` 路由）、`.ralph/lib/common.sh`（基础函数）、`scripts/check.sh`、`.gitignore`、`package.json`。

# 核心变更

- 按 `.spec/rules/readme.md` / `architecture.md` / `requirements.md` 把 `docs/` 重整理为标准结构：新建 `docs/requirements.md`（项目级目标、跨模块约束、模块索引）；把原 `docs/ralph/design.md` 拆成 `docs/architecture/overview.md`（CLI、运行目录、adapter 契约、退出原因、stagnation、lock、错误诊断类别、方案取舍）；`docs/ralph/provider-sessions.md` 搬到 `docs/architecture/integrations.md`（三家 provider 命令、session 路径、错误诊断关键字、UUID 依赖）；新建 `docs/architecture/security.md`（信任域、approval/sandbox 固定策略、`.env` 解析约束、secrets 禁入规则、攻击面与缓解）；删除整个 `docs/ralph/`。
- Adversarial review 修正 4 项必须修复：
  1. 退出原因由"6 种"统一为"7 种"——REQ-012、SC-012-1、`handoff.md` 稳定决策段、`overview.md` 表格对齐；`interrupted` 在 lock 前触发降级为"不产生 run 目录"已显式写入。
  2. REQ-011 / SC-011-1 / BPF-002 追加 Claude UUID 三路校验；与 `integrations.md` "启动校验阶段" 说法对齐。
  3. `integrations.md` Codex 错误诊断映射改写成 auth → rate_limit → quota → api 四条互斥优先级，消除 `429`/`rate_limit` 归 `quota` 的歧义。
  4. `task.md` T1 范围列出 13 个具体集成测试用例（7 退出原因 + 6 启动校验）并绑定 fake adapter 场景；`roadmap.md` T1 段引用 task.md，不再重述。
- 同步刷新全量引用：`README.md` / `AGENTS.md` / `docs/README.md` / `docs/roadmap.md` / `docs/requirements/ralph-loop/requirements.md` / `handoff.md` / `task.md` 中的 `docs/ralph/*` 路径替换为 `docs/requirements.md` + `docs/architecture/*`。
- `scripts/check.sh` 删除旧"已迁移"断言，追加 `docs/architecture/{overview,integrations,security}.md` 存在性与 `docs/ralph` 不存在断言。

# 影响文件或模块

- 新增：`docs/requirements.md`、`docs/architecture/overview.md`、`docs/architecture/integrations.md`、`docs/architecture/security.md`、`docs/checkpoints/2026-04-24-01-t1-ready.md`。
- 删除：`docs/ralph/design.md`、`docs/ralph/provider-sessions.md`、`docs/ralph/requirements.md`、`docs/ralph/roadmap.md`、`docs/ralph/`（目录）。
- 修改：`README.md`、`AGENTS.md`、`docs/README.md`、`docs/roadmap.md`、`docs/requirements/ralph-loop/requirements.md`、`handoff.md`、`task.md`、`scripts/check.sh`。
- 工具骨架不变：`.ralph/bin/ralph` 仅 `help` 路由；`.ralph/lib/common.sh` 基础函数；`.ralph/lib/run.sh` / `tasks.sh` / `session.sh` / `adapter-fake.sh` 仍未创建（T1 范围）。

# 稳定决策

- 22 条 v0.1 决策（独立演进、per-workspace 部署、`.env` 路径写死 `.ralph/.env`、不接受 `--cwd`、每轮 fresh oneshot、三 provider + 三函数 adapter、默认 `max_iter=0` / `timeout=0` / `stagnation_limit=5`、PROMPT 硬契约、approval 写死等）已在 `docs/requirements/ralph-loop/requirements.md` 的澄清记录和澄清结论段完整追踪。
- 退出原因枚举 7 种定稿；Claude 白名单 `"Bash,Read,Edit,Write,Glob,Grep"` 固定；Codex 错误诊断按四级优先级；UUID 校验归入 REQ-011 启动校验。
- 本仓库是 ralph-loop 工具的开发工程，不自用 ralph 驱动自身开发；`.ralph/PROMPT.md` / `.ralph/TASKS.md` 不入仓。

# 验证结果

- 命令：`bash scripts/check.sh`
- 结果：通过（`ralph-loop check passed`）
- 诊断：文档结构、`.ralph/bin/ralph help` 入口、新架构文档存在性与 `docs/ralph` 不存在检查全部通过；`git diff --check` 未运行（全量 untracked，非 diff 场景，无冲突标记风险）。

# Postmortem Sweep

- 结果：无需要新增。
- 关联：无。
- 说明：本轮的"退出原因 6 vs 7""UUID 启动校验事实源漂移""Codex 诊断映射歧义""集成测试用例数含糊"四项是 adversarial-review 发现的一次性文档不一致，已在同一会话内沿 `.spec/rules/adversarial-review.md` 流程修正；不属于可复用 agent 行为失败模式，不沉淀为 postmortem。

# 未验证范围与风险

- `ralph run` 主循环、7 个退出原因路径、6 个启动校验用例、fake adapter 四场景、Claude UUID 校验路径均未实现，集成测试脚本 `scripts/integration-test.sh` 尚未创建。以上全部在 T1 范围内，完成后才能端到端验证 REQ-001 ~ REQ-016 的大部分 SC-*。
- `docs/architecture/testing.md` 暂缺，计划在 T1 集成测试脚本成形后沉淀长期测试规范；当前验收靠 `scripts/check.sh` 的结构性回归。
- `docs/architecture/{api,backend,frontend,database,ui,deployment}.md` 标 "当前不适用" 但未写重新评估条件（adversarial review 可观察风险 #6），未修。

# 下一步

- 按 `task.md` T1 范围推进：`common.sh` 扩展（`.env` 解析 / workspace 自定位 / UUID fallback / flock / changed_files 收集器） → `tasks.sh` → `session.sh` → `adapter-fake.sh`（`RALPH_FAKE_SCENARIO` 四场景） → `run.sh`（主循环）→ `.ralph/bin/ralph` 接 `run` 路由 → `scripts/integration-test.sh`（13 用例）。
- T1 完成后更新 `handoff.md` 指向 T2（Claude adapter），并按需创建新 checkpoint。
