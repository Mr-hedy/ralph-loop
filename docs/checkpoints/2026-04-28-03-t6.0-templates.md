# 目标与约束

- 保留 T6.0（PROMPT.md + TASKS.md 参考样板入仓 `.ralph/`）完成的可用状态，作为 T6.1 stagnation 修正的起点锚点。
- 约束：仅新增两份样板文件 + check.sh 断言翻转 + docs/README.md 索引行；未改任何内核代码。

# 范围

- `.ralph/PROMPT.md`：部署单元参考样板，provider-agnostic loop protocol（身份澄清 / Procedure / TASKS.md 自修改规则 / 退出语义 / Git 礼仪 / fresh oneshot 约定 / 不做事清单）。
- `.ralph/TASKS.md`：最小 hello-world 起手示例（1 条 `[x]` 任务 + 1 条带子 bullet 任务 + 1 条单行任务）。
- `scripts/check.sh`：`test ! -f .ralph/PROMPT.md` / `test ! -f .ralph/TASKS.md` 两条"待创建"守卫翻转为 `test -f`（T6 prep 预设，T6.0 完成后即翻转）。
- `docs/README.md`：权威事实源表加"部署单元样板"行，引用 `.ralph/PROMPT.md` / `.ralph/TASKS.md`，说明 `cp -r` 一次性部署语义。

# 核心变更

- `.ralph/PROMPT.md`（新增，74 行 ≤120 行上限）：去 trantor 化的 ralph-agnostic 循环协议样板。所有规则可独立成立，不引用 `ralph status` / `ralph doctor` 等未实现功能。顶部注释明确"参考样板，ralph 内核不依赖此文件存在或内容形态"。
- `.ralph/TASKS.md`（新增，11 行 ≤20 行上限）：hello-world 起手示例，含结构说明注释（ralph 只识别顶层 `- [ ]` / `- [x]`，子 bullet 给 agent 读不进解析）。
- `scripts/check.sh`：两条守卫断言翻转（`test ! -f` → `test -f`），check.sh 重新通过。
- `docs/README.md`：部署单元样板索引行，说明 REQ-017 部署语义。

# 影响文件或模块

- `.ralph/PROMPT.md`（新增）
- `.ralph/TASKS.md`（新增）
- `scripts/check.sh`
- `docs/README.md`

# 稳定决策

- **PROMPT.md + TASKS.md 作为部署单元样板入仓**（REQ-017）：两文件随 `cp -r .ralph/ <workspace>/.ralph/` 带走，使用者按需裁剪；ralph 内核不读取这两个文件作为内核控制信号。
- **check.sh 守卫语义**：T6 prep 时写入的"待创建"守卫（`test ! -f`）在对应子任务完成后即翻转为"已存在"守卫（`test -f`）；这是预期维护模式，非回归。

# 验证结果

- 命令：`bash scripts/check.sh`
- 结果：通过
- 诊断：`ralph-loop check passed`；PROMPT.md 74 行（≤120）；TASKS.md 11 行（≤20）；两文件均已出现在 `.ralph/` 下；无 `runs/` / `lock` / `status.json` / `.env`（`git ls-files .ralph/` 验证）；`git diff --check` 无空白错误；本轮无代码改动，未跑 `integration-test.sh`（不必要）。

# Postmortem Sweep

- 结果：无需要新增
- 关联：无
- 说明：本轮改动符合预期——check.sh 守卫翻转是 T6 prep 已知的维护动作，不是失败或回归；PROMPT.md / TASKS.md 内容从 trantor 样板提炼并全面去 trantor 化，无约定违反。既有 PM-0001（macOS 兼容，本轮无 shell 代码改动）、PM-0002（跨任务决策沉淀，本轮 check.sh 翻转属于维护非决策）、PM-0003（任务收口 REQ traceability，T6.0 无需 traceability rerun；首次实战在 T6.6）均已检查，均无触发。

# 未验证范围与风险

- PROMPT.md 样板能否让真实 Claude 跟住协议（T6.3 多轮 smoke 间接验证）。
- REQ-017 部署链路（`cp -r .ralph/ <workspace>/.ralph/` 跑通 SC-017-1）未实证（T6.3 验证）。
- T6.1 stagnation 修正未开始，T6.2 effort 接入未开始；T6.3 smoke 前置依赖未就绪。

# 下一步

1. 进入 T6.1：stagnation 判据改为"本轮 vs 上轮"（方案 B：in-memory 哈希），新增 `partial_progress` mock 场景 + ≥2 集成测试用例。
2. T6.1 完成后回查 PM-0003 验证 stagnation 修复有效。
3. T6.1 → T6.2 effort 接入（adapter-claude.sh 读 `$RALPH_EFFORT` + thinking-budget 翻译）→ T6.3 多轮 smoke。
